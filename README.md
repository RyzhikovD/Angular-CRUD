# Angular CRUD Model

## Development server

Run `npm install`, `npm install @ngxs/store`.

Run `npm run start` for a dev server. Navigate to `http://localhost:4200/books`. The app will automatically reload if you change any of the source files.
